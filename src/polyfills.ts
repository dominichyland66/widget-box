/***************************************************************************************************
 * BROWSER POLYFILLS
 */

// Angular itself and @angular/elements need this first:
import 'zone.js';

// Only needed if you support older browsers that don't have native customElements:
import '@webcomponents/custom-elements';
